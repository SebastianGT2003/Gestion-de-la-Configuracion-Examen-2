name: Docker Image GHCR CI

on:
  push
jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Generate version number
        id: version
        run: echo "::set-output name=version::${{ github.run_number }}"
      - name: Build and push the Image
        run: |
          docker login --username gestionconfiguracion --password ${{ secrets.REGISTRY_PASSWORD }} gestionconfiguracion.azurecr.io
          docker build . --tag gestionconfiguracion.azurecr.io/ema/petclinic:${{ steps.version.outputs.version }}
          docker push gestionconfiguracion.azurecr.io/ema/petclinic:${{ steps.version.outputs.version }}
